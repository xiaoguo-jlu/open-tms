# Open-TMS 测试用例 — 默认银行账户规则(v1.1)

**模块**: basedata (8081) — DefaultBankAccountRule
**版本**: v1.1 (2026-07-08)
**测试工程师**: QA
**依据 PRD**: `docs/prd/M1/M1-主体默认银行账户规则PRD-v1.1.md`
**依据 API**: `docs/api/basedata/02-default-bank-account-rules.md`
**依据 UX**: `docs/原型/M1/M1-默认银行账户规则UX原型.html`

---

## 一、测试范围

| 模块 | 功能 | 端口/路径 |
|------|------|-----------|
| basedata | 默认银行账户规则维护 | `/api/v1/default-bank-account-rules` (8081) |
| 前端 | DefaultBankAccountRuleList | `/basedata/default-bank-account-rule` |

### 测试类型
- [x] API 接口测试(11 端点)
- [x] UI 功能测试
- [x] E2E 测试(FX 联动)

### 优先级统计

| 优先级 | API 用例 | UI 用例 | E2E 用例 | 总计 |
|--------|---------|---------|---------|------|
| P0 | 14 | 5 | 2 | 21 |
| P1 | 6 | 3 | 0 | 9 |
| P2 | 4 | 2 | 0 | 6 |
| **合计** | **24** | **10** | **2** | **36** |

---

## 二、用例汇总

### 2.1 API 测试用例

| 编号 | 用例名称 | 接口 | 优先级 |
|------|---------|------|--------|
| TC_DBAR_001 | 分页查询成功(有数据) | POST /page | P0 |
| TC_DBAR_002 | 分页查询筛选(按主体/状态) | POST /page | P0 |
| TC_DBAR_003 | 详情查询成功(返回 lockToken) | GET /{id} | P0 |
| TC_DBAR_004 | 详情查询(id 不存在) | GET /{id} | P1 |
| TC_DBAR_005 | 详情查询(ruleNumber 字符串) | GET /{ruleNumber} | P1 |
| TC_DBAR_006 | 新增规则成功(生成 rule_number + lockToken + 审计日志) | POST / | P0 |
| TC_DBAR_007 | 新增规则失败(必填缺失) | POST / | P1 |
| TC_DBAR_008 | 新增规则失败(Active 重复唯一约束) | POST / | P0 |
| TC_DBAR_009 | 新增规则失败(priority 超 9999) | POST / | P0 |
| TC_DBAR_010 | 更新规则成功(lockToken 一致) | POST /update | P0 |
| TC_DBAR_011 | 更新规则失败(lockToken 不一致 → 409) | POST /update | P0 |
| TC_DBAR_012 | 删除规则成功(软删 + 审计日志) | POST /delete/{id} | P0 |
| TC_DBAR_013 | 启用/停用切换正确 + 审计 | POST /{id}/enable & /disable | P0 |
| TC_DBAR_014 | ★ 双方向 match(返回 inflow + outflow) | GET /match | P0 |
| TC_DBAR_015 | match 缓存命中(同参数二次 < 20ms) | GET /match | P0 |
| TC_DBAR_016 | 规则变更清空 match 缓存 | GET /match | P0 |
| TC_DBAR_017 | match 无匹配兜底(返回 matched=false) | GET /match | P0 |
| TC_DBAR_018 | match 单方向(dualDirection=false) | GET /match | P1 |
| TC_DBAR_019 | match 测试匹配(返回所有命中) | GET /test-match | P1 |
| TC_DBAR_020 | 审计日志查询(分页) | GET /{id}/audit-logs | P0 |
| TC_DBAR_021 | 被引用数查询 | GET /{id}/reference-count | P1 |
| TC_DBAR_022 | priority 边界值 0 | POST / | P2 |
| TC_DBAR_023 | priority 边界值 9999 | POST / | P2 |
| TC_DBAR_024 | 优先级排序(priority DESC + created_at ASC) | POST / | P0 |

### 2.2 UI 测试用例

| 编号 | 用例名称 | 页面 | 优先级 |
|------|---------|------|--------|
| UI_DBAR_001 | 列表页查询/筛选/分页 | /basedata/default-bank-account-rule | P0 |
| UI_DBAR_002 | 新增规则(5 维选择 + 默认账户联动) | 同上 | P0 |
| UI_DBAR_003 | 编辑规则(lock banner + 409 处理) | 同上 | P0 |
| UI_DBAR_004 | 行内优先级编辑 + 状态切换 | 同上 | P0 |
| UI_DBAR_005 | 删除确认弹窗显示被引用 N | 同上 | P0 |
| UI_DBAR_006 | 复制规则 | 同上 | P1 |
| UI_DBAR_007 | 审计历史对话框 | 同上 | P0 |
| UI_DBAR_008 | 表单验证(必填/范围/锁定提示) | 同上 | P0 |
| UI_DBAR_009 | 列表为空状态 | 同上 | P2 |
| UI_DBAR_010 | 网络错误状态 | 同上 | P2 |

### 2.3 E2E 测试用例

| 编号 | 用例名称 | 流程 | 优先级 |
|------|---------|------|--------|
| E2E_DBAR_001 | 新建规则 → FX 录入联动(双方向自动带出) | basedata + dealing | P0 |
| E2E_DBAR_002 | 规则变更 → 已保存交易不受影响 | basedata + dealing | P1 |

---

## 三、详细测试用例

### 3.1 API 测试用例 — 11 端点

#### TC_DBAR_001 分页查询成功

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_001 |
| 测试类型 | API 功能测试 |
| 优先级 | P0 |
| 接口 | POST /api/v1/default-bank-account-rules/page |

**前置条件**
1. basedata 服务已启动(8081)
2. 数据库 `tms_default_bank_account_rule_t` 至少 5 条规则

**测试步骤**
1. 发送 POST 请求 `http://localhost:8081/opentms/basedata/api/v1/default-bank-account-rules/page`
2. 请求体: `{"pageNum":1,"pageSize":20,"managementEntityId":1}`

**预期结果**
- HTTP 200
- code=200,data.records.length ≥ 5
- data.total = 数据库总数
- 字段包含 id/ruleNumber/managementEntityId/counterpartyId/instrumentId/direction/currency/bankAccountId/status/priority/lockToken

---

#### TC_DBAR_002 分页查询筛选

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_002 |
| 优先级 | P0 |
| 接口 | POST /page |

**前置条件**: 至少 1 条 Active + 1 条 Inactive 规则

**测试步骤**
1. 发送 POST `/page`,body=`{"managementEntityId":1,"status":"Active"}`

**预期结果**
- 仅返回 status=Active 的规则
- 字段 status 全部为 "Active"

---

#### TC_DBAR_003 详情查询(返回 lockToken ★ v1.1)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_003 |
| 优先级 | P0 |
| 接口 | GET /api/v1/default-bank-account-rules/{id} |

**前置条件**: 已存在 ID=12 的规则

**测试步骤**
1. 发送 GET `http://localhost:8081/opentms/basedata/api/v1/default-bank-account-rules/12`

**预期结果**
- HTTP 200, code=200
- 响应包含 `lockToken`(UUID 字符串)
- 字段完整: id/ruleNumber/*5 维*/bankAccountId/status/priority/lockToken/createdBy/updatedAt/version

---

#### TC_DBAR_004 详情查询(id 不存在)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_004 |
| 优先级 | P1 |
| 接口 | GET /{id} |

**测试步骤**
1. GET `/api/v1/default-bank-account-rules/999999`

**预期结果**
- HTTP 404, code=4041001
- message: "规则不存在: 999999"

---

#### TC_DBAR_005 详情查询(ruleNumber 字符串)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_005 |
| 优先级 | P1 |
| 接口 | GET /{id} |

**测试步骤**
1. GET `/api/v1/default-bank-account-rules/RULE202607080001`

**预期结果**
- HTTP 200, code=200
- data.ruleNumber = "RULE202607080001"

---

#### TC_DBAR_006 新增规则成功(★ v1.1 5 维 + 审计)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_006 |
| 优先级 | P0 |
| 接口 | POST /api/v1/default-bank-account-rules |

**前置条件**
1. 主体 mgmt=1、对手方 cp=5001、产品 ins=401、账户 ba=1001 均存在

**测试步骤**
1. 发送 POST `/api/v1/default-bank-account-rules`,body:
```json
{
  "managementEntityId": 1,
  "counterpartyId": 5001,
  "instrumentId": 401,
  "direction": "Inflow",
  "currency": "USD",
  "bankAccountId": 1001,
  "priority": 100,
  "status": "Active",
  "description": "TC_DBAR_006 测试"
}
```

**预期结果**
- HTTP 200, code=200
- data.ruleNumber 格式 RULEyyyyMMddxxxx
- data.lockToken 是 UUID
- 审计日志表新增 1 条(operation=CREATE,old_value=null,new_value 完整快照)
- match 缓存被清空

---

#### TC_DBAR_007 新增规则失败(必填缺失)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_007 |
| 优先级 | P1 |
| 接口 | POST / |

**测试步骤**
1. POST `/`,body=`{"managementEntityId":1,"direction":"Inflow"}`(缺 bankAccountId)

**预期结果**
- HTTP 400, message 包含"默认账户必填"

---

#### TC_DBAR_008 新增规则失败(Active 重复唯一约束 ★ P1-3)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_008 |
| 优先级 | P0 |
| 接口 | POST / |

**前置条件**
- 已存在规则: mgmt=1, cp=5001, ins=401, dir=Inflow, cur=USD, status=Active

**测试步骤**
1. POST `/`,body=与已有规则完全相同的 5 维 + status=Active

**预期结果**
- HTTP 400, code=4001002
- message: "Active 规则维度组合已存在(包含已存在的 RULE...)"

---

#### TC_DBAR_009 新增规则失败(priority 超 9999 ★ P1-2)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_009 |
| 优先级 | P0 |
| 接口 | POST / |

**测试步骤**
1. POST `/`,body=priority=15000

**预期结果**
- HTTP 400, code=4001001
- message: "优先级超出范围 0-9999"

---

#### TC_DBAR_010 更新规则成功(lockToken 一致 ★ P0-3)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_010 |
| 优先级 | P0 |
| 接口 | POST /api/v1/default-bank-account-rules/update |

**前置条件**: 已有规则 ID=12, lockToken=abc-123

**测试步骤**
1. POST `/update`,body=`{"id":12,"lockToken":"abc-123","priority":200,"direction":"Inflow",...}`

**预期结果**
- HTTP 200, code=200
- 新 lockToken(不同于 abc-123)
- version+1
- 审计日志表新增 1 条 UPDATE,old_value 包含旧 priority,new_value 包含新 priority

---

#### TC_DBAR_011 更新规则失败(lockToken 不一致 → 409 ★ P0-3)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_011 |
| 优先级 | P0 |
| 接口 | POST /update |

**前置条件**: 规则 ID=12, 当前 lockToken=abc-123(实际值)

**测试步骤**
1. POST `/update`,body=`{"id":12,"lockToken":"WRONG-TOKEN","priority":200,...}`

**预期结果**
- HTTP 409, code=4091001
- message: "规则已被他人修改(updated_at=...),请刷新后重试"

---

#### TC_DBAR_012 删除规则成功(软删 + 审计)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_012 |
| 优先级 | P0 |
| 接口 | POST /delete/{id} |

**前置条件**: 已存在 ID=12 规则

**测试步骤**
1. POST `/delete/12`

**预期结果**
- HTTP 200, code=200
- 列表查询 GET /page 不再包含 ID=12
- DB 中 tms_default_bank_account_rule_t.deleted='1'
- 审计日志新增 1 条 DELETE

---

#### TC_DBAR_013 启用/停用切换

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_013 |
| 优先级 | P0 |
| 接口 | POST /{id}/enable & /{id}/disable |

**测试步骤**
1. POST `/12/disable` → 期望 status=Inactive
2. POST `/12/enable` → 期望 status=Active
3. 每次操作后检查审计日志(ENABLE / DISABLE 各 1 条)

**预期结果**
- 两次切换均成功
- 审计日志正确写入

---

#### TC_DBAR_014 ★ 双方向 match(★ P0-2 ★ v1.1 核心)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_014 |
| 优先级 | P0 |
| 接口 | GET /api/v1/default-bank-account-rules/match |

**前置条件**:
- 主体 1 下有 2 条规则:
  - R1: direction=Inflow, bankAccount=1001
  - R2: direction=Outflow, bankAccount=1002

**测试步骤**
1. GET `/match?managementEntityId=1&instrumentId=401&currency=USD&dualDirection=true`

**预期结果**
- HTTP 200, code=200
- data.inflow.matched=true, data.inflow.bankAccountId=1001
- data.outflow.matched=true, data.outflow.bankAccountId=1002
- inflow.ruleId ≠ outflow.ruleId
- inflow.priority 独立排序

---

#### TC_DBAR_015 match 缓存命中(< 20ms ★ P0-5)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_015 |
| 优先级 | P0 |
| 接口 | GET /match |

**前置条件**: TC_DBAR_014 通过

**测试步骤**
1. 第一次调用 match(冷启动)
2. 立即第二次调用 match(同参数)

**预期结果**
- 第二次响应时间 < 20ms(缓存命中)
- 两次响应数据完全一致

---

#### TC_DBAR_016 规则变更清空 match 缓存

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_016 |
| 优先级 | P0 |
| 接口 | GET /match |

**测试步骤**
1. 调用 match(cold start → 缓存写入)
2. 再次调用 match(warm → cache hit)
3. 修改任意规则(update 或 enable/disable)
4. 再次调用 match

**预期结果**
- 第 4 步不再是 cache hit(缓存被清空)
- 响应数据反映最新规则状态

---

#### TC_DBAR_017 match 无匹配兜底

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_017 |
| 优先级 | P0 |
| 接口 | GET /match |

**前置条件**: 没有任何匹配规则

**测试步骤**
1. GET `/match?managementEntityId=999&instrumentId=999`

**预期结果**
- HTTP 200, code=200
- data.inflow.matched=false, bankAccountId=null
- data.outflow.matched=false, bankAccountId=null

---

#### TC_DBAR_018 match 单方向

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_018 |
| 优先级 | P1 |
| 接口 | GET /match |

**测试步骤**
1. GET `/match?managementEntityId=1&instrumentId=401&direction=Inflow&dualDirection=false`

**预期结果**
- data.matched=true/false
- data.bankAccountId(单个)

---

#### TC_DBAR_019 test-match(返回所有命中)

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_019 |
| 优先级 | P1 |
| 接口 | GET /test-match |

**测试步骤**
1. GET `/test-match?managementEntityId=1&dualDirection=true`

**预期结果**
- data.matchedRules.length >= 1(所有命中)
- data.selectedInflow / selectedOutflow 标明首条

---

#### TC_DBAR_020 审计日志查询

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_020 |
| 优先级 | P0 |
| 接口 | GET /{id}/audit-logs |

**前置条件**: 规则 ID=12 已多次 UPDATE

**测试步骤**
1. GET `/12/audit-logs?pageNum=1&pageSize=20`

**预期结果**
- records 按 operatedAt DESC 排序
- 每条包含 operation/operator/operatedAt/oldValue(JSONB)/newValue(JSONB)

---

#### TC_DBAR_021 被引用数查询

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_021 |
| 优先级 | P1 |
| 接口 | GET /{id}/reference-count |

**测试步骤**
1. GET `/12/reference-count`

**预期结果**
- 返回 unsettledCount / recentSettledCount / totalCount
- (本期临时方案:全部为 0,P1+ 完善)

---

#### TC_DBAR_022 priority 边界值 0

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_022 |
| 优先级 | P2 |
| 接口 | POST / |

**测试步骤**: 新增规则 priority=0

**预期结果**: 创建成功

---

#### TC_DBAR_023 priority 边界值 9999

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_023 |
| 优先级 | P2 |
| 接口 | POST / |

**测试步骤**: 新增规则 priority=9999

**预期结果**: 创建成功

---

#### TC_DBAR_024 优先级排序

| 项目 | 内容 |
|------|------|
| 用例编号 | TC_DBAR_024 |
| 优先级 | P0 |
| 接口 | POST /page |

**前置条件**: 同维度组合下 3 条规则,priority=100/200/150,created_at 不同

**测试步骤**
1. GET `/match?managementEntityId=1&...&dualDirection=true`

**预期结果**
- 选中规则 priority=200(数字最大)
- 同优先级时按 created_at ASC 取最早

---

### 3.2 UI 测试用例

#### UI_DBAR_001 列表页查询/筛选/分页

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_001 |
| 优先级 | P0 |
| 页面 | /basedata/default-bank-account-rule |

**测试步骤**
1. 浏览器访问该页面
2. 选择管理主体(如"集团总部")
3. 选择状态 = Active
4. 点击"查询"

**预期结果**
- 表格只显示 Active 规则
- 表格列:规则编号/对手方/产品/方向/币种/默认账户/优先级/状态/生效日/操作
- 默认按 priority DESC 排序
- 分页器正确显示 total

---

#### UI_DBAR_002 新增规则(5 维 + 联动)

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_002 |
| 优先级 | P0 |
| 页面 | /basedata/default-bank-account-rule |

**测试步骤**
1. 点击"+ 新增规则"
2. 主体选择"集团总部"
3. 对手方、产品、方向、币种依次选择
4. 默认账户下拉只显示"集团总部"下的账户
5. 点击"保存"

**预期结果**
- 弹窗关闭,列表新增一条规则
- ruleNumber 自动生成 RULE...

---

#### UI_DBAR_003 编辑规则(lock banner + 409 处理)

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_003 |
| 优先级 | P0 |
| 页面 | 同上 |

**测试步骤**
1. 列表点击"编辑"
2. 修改 priority 后保存

**预期结果**
- 编辑对话框显示 lock banner:"此规则已加载 · 数据更新于..."
- 主体字段灰显
- 保存成功,列表自动刷新

**409 场景**:
1. 在 A 浏览器打开编辑,获取 lockToken=X
2. 在 B 浏览器修改同一规则,lockToken 变为 Y
3. A 浏览器提交(X)→ 提示"规则已被他人修改",弹窗让刷新

---

#### UI_DBAR_004 行内优先级编辑 + 状态切换

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_004 |
| 优先级 | P0 |
| 页面 | 同上 |

**测试步骤**
1. 在表格行内调整优先级(±10)
2. 切换状态 Switch

**预期结果**
- 优先级变化即时持久化(行内编辑无保存按钮)
- Switch 切换后立即生效,显示成功 toast

---

#### UI_DBAR_005 删除确认弹窗显示被引用 N

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_005 |
| 优先级 | P0 |
| 页面 | 同上 |

**测试步骤**
1. 列表点击"删除"
2. 弹出确认框

**预期结果**
- 显示"该规则已被 N 笔交易引用"(N=28 测试数据)
- 确认后软删,列表移除

---

#### UI_DBAR_006 复制规则

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_006 |
| 优先级 | P1 |
| 页面 | 同上 |

**测试步骤**
1. 列表点击"复制"
2. 编辑对话框打开,字段已预填,ruleNumber 为空

**预期结果**
- 主体不变(灰显)
- 描述前缀"[复制]"
- 保存时生成新 rule_number

---

#### UI_DBAR_007 审计历史对话框

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_007 |
| 优先级 | P0 |
| 页面 | 同上 |

**测试步骤**
1. 列表点击"审计"
2. 弹窗展示审计历史

**预期结果**
- 时间线展示 operation/operator/operatedAt
- 折叠面板可查看 oldValue/newValue 详情

---

#### UI_DBAR_008 表单验证

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_008 |
| 优先级 | P0 |
| 页面 | 同上 |

**测试步骤**
1. 新增规则,清空必填字段后保存
2. priority 输入 15000

**预期结果**
- 必填字段红色提示
- priority 超出范围时 InputNumber 阻止输入或显示错误

---

#### UI_DBAR_009 列表为空状态

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_009 |
| 优先级 | P2 |
| 页面 | 同上 |

**测试步骤**
1. 选择没有任何规则的主体

**预期结果**
- 显示空状态:"暂无规则" + "+ 新增规则" 按钮

---

#### UI_DBAR_010 网络错误状态

| 项目 | 内容 |
|------|------|
| 用例编号 | UI_DBAR_010 |
| 优先级 | P2 |
| 页面 | 同上 |

**测试步骤**
1. 停止 basedata 服务
2. 刷新页面

**预期结果**
- 显示"加载失败"提示 + "重试"按钮

---

### 3.3 E2E 测试用例

#### E2E_DBAR_001 新建规则 → FX 录入联动(★ 双方向)

| 项目 | 内容 |
|------|------|
| 用例编号 | E2E_DBAR_001 |
| 优先级 | P0 |
| 涉及模块 | basedata + dealing |

**前置条件**
1. basedata(8081) + dealing(8082) 服务均启动
2. 已有管理主体"集团总部"和其下的 FX-SPOT-USD-CNY 产品

**测试步骤**
1. 进入 basedata 默认账户规则
2. 新增规则:
   - 主体=集团总部,对手方=中国银行,产品=FX-SPOT-USD-CNY
   - 方向=Inflow, 币种=USD, 账户=中行美元 #1001
   - 再新增一条 Outflow, 币种=CNY, 账户=中行人民币 #1002
3. 切换到 dealing 模块
4. 进入 FX 录入(SPOT)
5. 选择主体=集团总部, 产品=FX-SPOT-USD-CNY, 币种对=USD/CNY

**预期结果**
- 收账账户(USD)自动填充为中行美元 #1001
- 付账账户(CNY)自动填充为中行人民币 #1002
- 300ms 防抖(快速切换产品时只有最后一次触发)

---

#### E2E_DBAR_002 规则变更不影响已保存交易

| 项目 | 内容 |
|------|------|
| 用例编号 | E2E_DBAR_002 |
| 优先级 | P1 |
| 涉及模块 | basedata + dealing |

**测试步骤**
1. 新增规则 R1,默认账户=账户 A
2. 录入 FX 交易 T1,match 自动填充账户 A
3. 保存 T1
4. 修改规则 R1 的默认账户为账户 B
5. 查看 T1 的详情

**预期结果**
- T1 仍显示账户 A(已保存的交易不受影响)
- 新的 FX 录入会使用账户 B

---

## 四、测试数据

### 4.1 规则测试数据

| rule_number | management_entity_id | counterparty_id | instrument_id | direction | currency | bank_account_id | status | priority |
|-------------|---------------------|-----------------|---------------|-----------|----------|-----------------|--------|----------|
| RULE202607080001 | 1 | 5001 | 401 | Inflow | USD | 1001 | Active | 200 |
| RULE202607080002 | 1 | 5001 | 401 | Outflow | CNY | 1002 | Active | 180 |
| RULE202607080005 | 1 | null | null | ALL | null | 1003 | Active | 50 |
| RULE202607050003 | 1 | 5002 | null | Inflow | EUR | 2003 | Inactive | 80 |

### 4.2 测试数据准备脚本

```sql
-- 插入测试规则
INSERT INTO tms_default_bank_account_rule_t
  (rule_number, management_entity_id, counterparty_id, instrument_id, direction, currency, bank_account_id, status, priority, created_by)
VALUES
  ('RULE202607080001', 1, 5001, 401, 'Inflow', 'USD', 1001, 'Active', 200, 'system'),
  ('RULE202607080002', 1, 5001, 401, 'Outflow', 'CNY', 1002, 'Active', 180, 'system'),
  ('RULE202607080005', 1, NULL, NULL, 'ALL', NULL, 1003, 'Active', 50, 'system'),
  ('RULE202607050003', 1, 5002, NULL, 'Inflow', 'EUR', 2003, 'Inactive', 80, 'system');

-- 清理
-- DELETE FROM tms_default_bank_account_rule_t WHERE rule_number LIKE 'RULE20260708%';
```

---

## 五、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Redis 不可用 | match 缓存降级为 ConcurrentHashMap | 单实例 OK,集群需 P1+ 升级 |
| lockToken 实现边界 | 30 分钟过期机制本期未实现 | 编辑期间持续锁定,关闭窗口需手动解锁 |
| 被引用 N 查询 | tms_deals_t 无 bank_account_id 列 | 临时返回 0,P1+ 完善 |
| Active 唯一约束 | 部分索引依赖 deleted='0' | 软删后约束自动失效 |

---

## 六、待确认事项

1. Q8:lockToken 过期时间 30 分钟是否合理?
2. Q9:Redis 不可用降级方案 OK?
3. Q10:被引用 N 的 90 天窗口是否合理?
4. audit log 表保留期?(目前未设,需确认数据归档策略)
5. 测试数据是否需要隔离环境?(当前共用 opentms 库)

---

## 七、执行优先级

**P0 必测(冒烟测试)**:
- TC_DBAR_001/002/003/006/008/009/010/011/012/013/014/015/016/017/020/024
- UI_DBAR_001/002/003/004/005/007/008
- E2E_DBAR_001

**P1 应测**: 其余 P1 用例

**P2 可选**: 边界值 + 异常状态

---

*QA 产出 - v1.1 (2026-07-08)*
*36 个测试用例:API 24 + UI 10 + E2E 2*
*重点验证 v1.1 关键升级:双方向匹配 + 并发控制 + Active 唯一约束 + 审计日志 + 缓存*