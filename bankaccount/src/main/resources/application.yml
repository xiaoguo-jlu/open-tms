server:
  port: 8083

spring:
  application:
    name: opentms-bankaccount
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/opentms
    username: opentms
    password: opentms123

mybatis-plus:
  global-config:
    db-config:
      id-type: auto